# Sao chép file này thành .env rồi chỉnh theo VPS của bạn:  cp .env.example .env
PORT=3000
HOST=0.0.0.0
ENABLE_MQTT=true
MQTT_TCP_PORT=1883
MQTT_WS_PORT=8883
CORS_ORIGIN=*
DB_PATH=data/twin.db
NODE_ENV=production
