{
  "name": "digital-twin-traffic-backend",
  "version": "1.0.0",
  "description": "Digital Twin Traffic System — Real Backend (Node.js)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --ignore public/ --ignore data/",
    "build:frontend": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
    "deploy": "npm install && npm run build:frontend && node server.js"
  },
  "dependencies": {
    "aedes": "^0.51.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "mqtt": "^5.10.1",
    "socket.io": "^4.7.5",
    "sql.js": "^1.14.1",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
